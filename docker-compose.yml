version: '3'
services:
  bot:
    build: .
    env_file: .env
    restart: always  # 自动重启（可靠性）
    networks:
      - bot-network
  # 可选：Redis缓存（提升性能）
  redis:
    image: redis:alpine
    restart: always
    networks:
      - bot-network

networks:
  bot-network: